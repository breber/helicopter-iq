enum {
    // We have a clean slate
    WAITING,
    // The user has indicated the game should start
    STARTING,
    // The game is in full swing
    RUNNING,
    // The game is over
    FINISHED
}
