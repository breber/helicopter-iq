class Block {
    function initialize(aHeight, aX, aY) {
        height = aHeight;
        x = aX;
        y = aY;
    }

    function contains(aX, aY) {
        // TODO: check if contains
        return false;
    }

    const width = 20;
    var height  = 0;
    var x       = 0;
    var y       = 0;
}
